{
  "name": "@rerun/root",
  "private": true,
  "version": "1.0.0",
  "keywords": [],
  "author": "Chap Callanan",
  "license": "GPL-3.0",
  "workspaces": [
    "./core",
    "./common",
    "./webgraphics"
  ],
  "scripts": {
    "build:common": "npm run build --prefix=common/",
    "build:webgraphics": "npm run build --prefix=webgraphics/",
    "build:core": "npm run build --prefix=core/",
    "build": "npm run build:common && npm run build:webgraphics && npm run build:core"
  }
}
